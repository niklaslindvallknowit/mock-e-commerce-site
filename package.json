{
  "name": "mock-e-commerce-site",
  "private": true,
  "workspaces": [
    "src/frontend"
  ],
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"blue,green\" \"dotnet run --launch-profile http --project src/backend/MockEcommerce.Api\" \"npm run dev --workspace src/frontend\"",
    "test:frontend": "vitest run",
    "test": "vitest run"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@vitejs/plugin-react": "^6.0.1",
    "@vitest/coverage-v8": "^4.1.4",
    "concurrently": "^9.1.2",
    "jsdom": "^29.0.2",
    "vitest": "^4.1.4"
  }
}
